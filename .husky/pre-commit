# If you want to skip running this husky hook, uncomment the next line
# exit 0

# Run linting and formatting checks on staged files
npx lint-staged

# Check for unused dependencies and exports
npm run knip

# Run tests non-interactively
CI=true npm run test:all
